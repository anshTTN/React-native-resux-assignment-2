import {Text, View} from 'react-native';
import React from 'react';
import ChannelCard from './ChannelCard';

const Home = () => {
  return (
    <View style={{flex: 1}}>
        <ChannelCard />
    </View>
  );
};

export default Home;
